# Style: default

No style override. Behave as vanilla Claude Code with the user's CLAUDE.md instructions and any active superpowers/skills as the only guidance.

(This file is intentionally minimal. The `style-activate` hook treats `default` as a no-op and injects nothing.)
